<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="asset_asset_action_view_form" model="ir.ui.view">
        <field name="name">asset_asset.form</field>
        <field name="model">asset.asset</field>
        <field name="arch" type="xml">
            <form string="Asset">
                <sheet>
                    <group>
                        <group>
                            <field name="number"/>
                            <field name="model_id"/>
                            <field name="serial_no"/>
                            <field name="location"/>
                        </group>
                        <group>
                            <field name="employee_id"/>
                            <field name="department_id"/>
                            <field name="install_date"/>
                            <field name="warranty_date"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" groups="base.group_user"
                           options="{'post_refresh': 'recipients'}"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>

            </form>
        </field>
    </record>

    <record model="ir.ui.view" id="asset_asset_action_view_list">
        <field name="name">Asset models</field>
        <field name="model">asset.asset</field>
        <field name="arch" type="xml">
            <tree>
                <field name="department_id"/>
                <field name="model_id"/>
                <field name="number"/>
                <field name="serial_no"/>
                <field name="location"/>
                <field name="install_date"/>
                <field name="warranty_date"/>
            </tree>
        </field>
    </record>

    <record model="ir.actions.act_window" id="asset_asset_action">
        <field name="name">Assets</field>
        <field name="res_model">asset.asset</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
